# HP-pool combat (ADR-0005 subsystem `combat`; Fantasy-flagship v0.1,
# combat decisions ratified 2026-06-24: round-per-roll, hybrid damage
# (weapon die + margin nudge), sheet hp). A prompt-fragment module —
# combat reuses the d100 check loop from the resolution module (an attack
# IS a Body check vs the target's Defense), so it adds no new roll
# infrastructure. RFC-0007.

name = "core/hp-pool-v1"
version = "1.1.0"
subsystem = "combat"
interface_version = "1.0"

prompt_fragment = "prompt.md"

# RFC-0014: module_data.combat now carries the death-save clock
# (death_saves_failed). Additive — interface_version stays 1.0.
schema_fragment = "schema.json"
preset_paths = []
# Combat reads the four stats + class HP factor and reuses the d100 check.
requires = ["core/four-stat-v1", "core/four-class-fantasy-v1", "core/d100-open-v1"]
