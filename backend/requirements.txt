fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-dotenv>=1.0
# Transitive dependency: backend imports engine, and engine/llm.py uses the
# openai SDK. Kept here so `just install-backend` in isolation still produces
# a working backend without requiring a separate `just install-engine` step.
openai>=1.0
